const initialState = [];

export default (state = initialState, action) => {
    let nextState = {...state};

    switch (action.type) {
        default: {
        }
        break;
    }

    return nextState;
}
